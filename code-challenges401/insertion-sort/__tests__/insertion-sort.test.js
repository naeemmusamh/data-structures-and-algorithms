const SelectionSort = require('../insertion-sort.js');

// describe('test if the function work correctlly', () => {
//     it('test if the the function Sort the array', () => {
//         let array = [8, 4, 23, 42, 16, 15];
//         expect(SelectionSort(array)).toEqual([4, 8, 15, 16, 23, 42]);
//     });
// });